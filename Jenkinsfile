@Library('shared-jenkins-pipelines') _

poetry(
    docker: true,
    dockreg: "git.sudo.is/ben",

    pip_publish: true,
    pip_registry: "https://git.sudo.is/api/packages/ben/pypi"
    // default:
    //pip_registry: "pypi"

    //push_git_tag: true,
)
